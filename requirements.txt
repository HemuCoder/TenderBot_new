# ==========================================================
# TenderBot - Python 依赖项
# ----------------------------------------------------------
# 本文件列出了运行 'tender_analysis' 模块及其关联服务
# 所需的所有 Python 软件包。
#
# 若要安装所有依赖项，请在项目根目录下运行以下命令：
#
# pip install -r requirements.txt
# ==========================================================

# 用于构建 API 的核心 FastAPI 框架
fastapi

# 用于运行 FastAPI 应用程序的 ASGI 服务器
uvicorn==0.30.1

# 用于构建和编排 LLM Agent 的核心 SDK
# 我们使用 'litellm' 附加功能进行安装，以获得广泛的模型提供商支持。
openai-agents[litellm]

# 用于连接到 MCP (主控程序) 服务的客户端库
fastmcp

# 用于通过环境变量和 .env 文件进行健壮的设置管理
pydantic-settings==0.2.0
python-dotenv==1.0.1

# 用于在本地将 .docx 文件转换为 Markdown
python-docx

# 用于精确计算文本中的 Token 数量，以实现智能分块
tiktoken
