整个解标模块的设计请严格遵守jr-tenderbot-quotation-master文件下的结构，核心的逻辑由以下两个文件来承载：
# analysis_agents.py：放置当前模块下全部的agent及提示词内容，并整合成为函数，供业务逻辑调用

分别有七个agent：
- 商务要求提取agent
- 技术要求提取agent
- 报价要求提取agent
- 评分要求提取agent
- 模版提取agent
- 非标模版提取agent
- 清单整合agent

agent提示词留占位符，我稍后会一个个补充。


# orchestrator.py：存放当前模块的全部业务逻辑

- 首先读取一个本地word文件：/Users/cris/Documents/JR/Agent_py/TenderBot_New/jr_tenderbot_mcp/mcp-file/data/国投健康产业投资有限公司技术人力服务外包资源池建设项目-招标文件.docx，将文档转换成md格式，存放到工作路径中。
- 将文件转成md然后按照标题结构进行分块，切割成合适的上下文大小，便于后续agent的分析，这个逻辑参考document_processor.py文件中的逻辑编写，这个文件中的内容是已经测试过的，你只需要把内容适配到当前项目中。
- 商务、技术、报价、评分、模版agent分别循环上一环节中生成的多个文本块，
    - 其中每次把调用商务、技术、报价、评分Agent生成的结果通过mcp中的文件写入工具，把每个agent输出的内容添加到一个md文档中，保存在mcp工具的工作文件夹下。
    - 模版agent需要输出标准的json格式文件，同样放到mcp工作文件夹下。
- 代码检查模版提取agent输出的json文件中哪些没有提取到模版的"key_sentence",找到这些模版，循环调用非标模版提取agent（输入：没有提取到的模版来源文本块，重新提取相关模版，通过关键词定位），历史版本的代码参考template_anchor_extractor.py文件。
- 最终将模版提取agent输出的json文件中有"key_sentence"的部分，和非标模版提取agent输出的json文件合并成一个大的json文件，合并后的参考example.json文件中的数据格式。
- 清单整合是最终环节，需要对上述环节生成的所有内容进行整合而生成最终的一个需求清单供后续应用。
    - 整个解标部分的要求分为，商务部分要求、技术部分要求、以及报价部分要求
    - 我的想法是首先读取“评分要求分析”的文件内容，让Agent先根据评分要求弄出一个最终清单的大纲，这个大纲一定是作为后续投标文件中的内容能拿到满分的指导文件内容
    - 然后再依次调用不同的章节分析文件，和第一步生成的章节大纲，完善整体的清单内容
    - 清单要以这样的格式"- [ ] 投标....."，每个大的模块下可以有小标题




生成目录要求：
- 投标文件中是否有投标文件格式要求，如有按照第一优先级，将招标文件中的投标文件格式目录提取出来：
    - 需要提取出json格式的目录结构

- 根据final_checklist.md需求清单中的# 商务部分评分+上一步提取出的投标文件格式，生成商务部分详细目录
    - 提取出来的内容可能是这样"""
    1.投标函
    2.授权委托书
    3.联合体协议书（不适用）
    4.投标保证金
    5.资格审查资料
    6.其他文件和资料
    """很多的材料比如证书等等，都需要放到资格审查或者其他文件和资料的标题下，这个环节就是要把要求中的一些内容填充到一次提取出来的目录框架下，并增加一些需求说明。
    - 最后部分目录，比如"一、投标函"，这个投标函就是需要解标环节提取的模版，需要把这个目录，关联上投标函模版
    - json字段我现在能想到的是要包含目录名称、内容描述、还有目录的层级结构、目录关联的模版

- 提取投标文件格式agent
- 目录生成总控agent
- 

- 根据final_checklist.md需求清单中的# 商务部分评分，生成商务部分详细目录
    - 根据需求清单中的评分标准检查这个目录得分情况，目标是要拿满分

- 根据历史参考项目生成技术部分目录
    - 检查评分项，目标拿满分
- 根据投标文件格式生成报价部分目录大纲



